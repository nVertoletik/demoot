version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "8080:8080"
    networks:
      - app-network

  client:
    build: ./client
    depends_on:
      - server
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
